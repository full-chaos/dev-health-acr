apiVersion: v1
kind: Service
metadata:
  name: acr-api
  labels:
    app.kubernetes.io/name: acr
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: acr
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
