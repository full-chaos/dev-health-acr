# Companion valid file

Present so the fixture directory's overall render check has something real to
pass on -- the point of this fixture is that `tab-indented.md`'s block is
never counted at all, not that the whole tree has zero blocks.

```mermaid
flowchart TB
    A --> B
```
